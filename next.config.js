/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // Enable React strict mode for highlighting potential issues
  reactStrictMode: true,
  // Future‑proof the app by enabling SWC minification
  swcMinify: true
};

module.exports = nextConfig;